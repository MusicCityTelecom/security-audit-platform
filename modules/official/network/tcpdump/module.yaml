schema_version: 1
id: official.network.tcpdump
name: tcpdump
version: 0.1.0
category: Network
runtime: wsl
entrypoint: tcpdump
capabilities:
  - network
privileges:
  - network
requires:
  tools:
    - tcpdump
  hardware: []
  runtimes:
    - wsl
network:
  behavior: active
execution:
  executable: tcpdump
  arguments:
    - "-D"
  timeout_seconds: 3600
license:
  spdx: UNKNOWN
source:
  type: upstream
  repository: https://github.com/the-tcpdump-group/tcpdump
  ref: main
evidence:
  - stdout
  - stderr
